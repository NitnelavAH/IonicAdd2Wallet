import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nitnelav.pass.wallet',
  appName: 'IonicPassToWallet',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
